# ── Auth.js (verplicht) ─────────────────────────────────────
# Genereer: openssl rand -base64 32
AUTH_SECRET=

# Zelfde database als backend (Prisma adapter + credentials)
DATABASE_URL=postgresql://orion:orion@localhost:5432/orion_dev

# Zelfde geheime string als backend/.env
ORION_BFF_SECRET=

# ── API ─────────────────────────────────────────────────────
NEXT_PUBLIC_ORION_API_BASE=http://127.0.0.1:4000
# ORION_API_BASE=http://127.0.0.1:4000
